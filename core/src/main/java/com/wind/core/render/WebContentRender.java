package com.wind.core.render;

public class WebContentRender extends AbstractRenderMethod {
	@Override
	public String format(String plainAnswer) {
		return "[WebContentRender] format : " + plainAnswer;
	}
}
