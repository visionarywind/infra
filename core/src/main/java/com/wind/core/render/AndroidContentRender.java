package com.wind.core.render;

public class AndroidContentRender extends AbstractRenderMethod {
	@Override
	public String format(String plainAnswer) {
		return "[AndroidContentRender] format : " + plainAnswer;
	}
}
